if Framework == 'QBCore' then

    -- QBCore-specific client functions (e.g., using QBCore notifications, which are often handled server-side)
    -- This section would be used for client-side calls that are specific to QBCore.

end